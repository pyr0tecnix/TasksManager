language: node_js

node_js:
  - 'stable'

#Echoue dès qu'une erreur intervient

matrix:
  fast_finish: true

cache:
  directories:
    - node_modules

before_install:
  - npm prune

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

script:
  npm test

#Variables d'environnement
env:
  global:
    - NODE_ENV=production
